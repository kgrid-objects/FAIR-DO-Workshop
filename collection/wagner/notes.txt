Explor other platforms that let users use a jupiter notebook file without a login requirement. for example the following two:
upyterLite (Try Jupyter): Runs entirely inside your web browser using WebAssembly. It requires no login or setup, but data stays in your browser session and does not use a remote server.
MyBinder: Lets you launch a live, temporary Jupyter server instantly from a public GitHub repository without any login


Add other artifacts:
- metadata (FS)
- jupiter notebook (FS)
- documentation (AF)

Maybe create a demo (the notebook might be sufficient)
create the second ko